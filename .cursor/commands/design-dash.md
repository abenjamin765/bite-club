---
description: Run the full nine-phase Design Dash workflow (P0–P8) from fuzzy problem to complete product plan.
---

# Design Dash

Read and follow `~/.cursor/skills/design-dash/SKILL.md` exactly.

Run the full nine-phase Design Dash (P0–P8). Supports optional flags:
- `--phase {N}` — resume at phase N (0–8)
- `--tier {express|standard|high-stakes}` — escalate tier only (never downgrade)

Write outputs to `dashes/{slug}/` and object guides to `library/objects/`. Use templates from `templates/`.
